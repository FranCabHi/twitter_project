<div class="container">
  <h1>Tweets</h1>
  <% if signed_in? && current_user != nil %>
    <%= render 'form', tweet: @tweet %> 
  <% end  %>

  <div class="container">
    <%= render 'shared/timeline' %> 
  </div>
</div>